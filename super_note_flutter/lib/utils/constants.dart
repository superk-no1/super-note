import 'package:flutter/material.dart';

class AppColors {
  static const backgroundColor = Color(0xFFF2F2F7);
  static const searchBarColor = Color.fromRGBO(216, 216, 216, 1);
  static const accentColor = Colors.orangeAccent;
}

class AppStyles {
  static const headerTextStyle = TextStyle(
    color: Colors.black,
    fontSize: 32,
    fontWeight: FontWeight.bold,
  );

  static const monthHeaderStyle = TextStyle(
    fontSize: 22,
    fontWeight: FontWeight.bold,
  );
}
